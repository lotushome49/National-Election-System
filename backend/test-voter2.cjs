const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function test() { const voter = await prisma.user.findFirst({ where: { roleId: '47dcb5e1-8fec-4607-b35d-b2a65d648212' } }); if (!voter) return console.log('No voter found'); console.log('Voter:', voter.username); const res = await fetch('http://localhost:3001/api/v1/auth/login', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ username: voter.username, password: 'password123' }) }); const data = await res.json(); const token = data.data.accessToken; console.log('Token:', token ? 'yes' : 'no'); const res2 = await fetch('http://localhost:3001/api/v1/elections/current/open', { headers: { 'Authorization': 'Bearer ' + token } }); console.log('Status:', res2.status); const data2 = await res2.json(); console.log('Response:', JSON.stringify(data2)); await prisma.\$disconnect(); } test().catch(console.error);